import java.util.*;

 class arrbetweensum

 {

   public static void main(String args[])

   { 

    System.out.println("Enter size of array");

    Scanner k= new Scanner(System.in);

    int n=k.nextInt();

    int a[]= new int[n];

    System.out.println("Enter array elements");

    for(int i=0;i<n;i++)

    a[i]=k.nextInt();

    System.out.println("Enter left index and right index");

    int li=k.nextInt(), ri=k.nextInt(), result=0;

    if((n>li) && (n>ri) && (li<=ri)){

      for(int i=li;i<=ri;i++)

      result=result+a[i];

      System.out.println("The sum between the given range  is "+result);

    }

    else 

     System.out.println("sum not possible incorrect index");   

   }

 }
 
 
 Output:
 Enter size of array

4

Enter array elements

6

7

8

9

Enter left index and right index

2

3

The sum between the given range  is 17

Process finished.
